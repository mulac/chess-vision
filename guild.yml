qtrain:
  main: chessvision.train
  flags-import: all
  sourcecode:
    - '*.py'
  requires:
    - file: games/
      target-type: link

train:
  main: chessvision.train
  flags-import: all
  sourcecode:
    - '*.py'

